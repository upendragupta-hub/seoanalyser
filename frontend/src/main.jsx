import React from 'react'
import ReactDOM from 'react-dom/client'
import App from './App' // Agar App.jsx hai toh

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <App />
  </React.StrictMode>,
)